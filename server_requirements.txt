openai-whisper

# Use PyTorch CUDA 12.8 build. transformers now hard-requires torch>=2.6
# (CVE-2025-32434) to load non-safetensors checkpoints via torch.load, so
# pin a version satisfying that rather than an unpinned "torch" (which can
# resolve to a version this index doesn't have a CUDA wheel for and silently
# fall back to a CPU-only build from PyPI).
--index-url https://download.pytorch.org/whl/cu128
torch==2.9.1+cu128
torchvision==0.24.1+cu128
torchaudio==2.9.1+cu128

# Switch back to PyPI for the rest
--extra-index-url https://pypi.org/simple

ultralytics>=8.2
mediapipe>=0.10.14
transformers>=4.43
sentencepiece>=0.2.0  # required by XLM-RoBERTa (sentiment) tokenizer
protobuf  # required by XLM-RoBERTa (sentiment) tokenizer's SentencePiece extractor
opencv-python>=4.9
pyyaml>=6.0
tqdm>=4.66
orjson>=3.9
numpy>=1.26
pyyaml
fastapi
uvicorn[standard]
pydantic
librosa
python-multipart

pytest>=8.0  # for `pytest` (tests/)